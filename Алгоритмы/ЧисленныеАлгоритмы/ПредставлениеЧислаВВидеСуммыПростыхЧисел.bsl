// Представление числа в виде суммы простых чисел (например, для 10: 7 + 3)

Функция ПростоеЧисло(Знач n) 
    Если n < 2 Тогда
        Возврат Ложь; // Числа меньше 2 не являются простыми
    КонецЕсли;

    Для i = 2 По Цел(n / 2) Цикл
        Если n % i = 0 Тогда
            Возврат Ложь; // Если есть делитель, число не простое
        КонецЕсли;
    КонецЦикла;

    Возврат Истина; // Число простое
КонецФункции

Процедура ПроверитьСуммуПростых(Знач Число) 
    Флаг = Ложь;

    // Перебираем числа от 2 до Число / 2
    Для i = 2 По Цел(Число / 2) Цикл
        Если ПростоеЧисло(i) И ПростоеЧисло(Число - i) Тогда
            Сообщить("Число " + Строка(Число) + " можно представить как сумму " + Строка(i) + " и " + Строка(Число - i));
            Флаг = Истина;
            Прервать; // Достаточно найти хотя бы одну пару
        КонецЕсли;
    КонецЦикла;

    Если НЕ Флаг Тогда
        Сообщить("Число " + Строка(Число) + " нельзя представить как сумму двух простых чисел.");
    КонецЕсли;
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
    Число1 = 10;
	Число2 = 20;
	Число3 = 30;
	Число4 = 40;

    ПроверитьСуммуПростых(Число1);
    ПроверитьСуммуПростых(Число2);
    ПроверитьСуммуПростых(Число3);
    ПроверитьСуммуПростых(Число4);

КонецПроцедуры
