// Подсчёт частоты каждого символа в строке

Функция НайтиЧастотыСимволов(Строка) 
    // Создаём структуру для хранения частот
    Частоты = Новый Соответствие;

    // Проходим по каждому символу строки
    Для Индекс = 1 По СтрДлина(Строка) Цикл
        Символ = Сред(Строка, Индекс, 1);

        // Увеличиваем счётчик для данного символа
        Если Частоты.Получить(Символ) = Неопределено Тогда
            Частоты.Вставить(Символ, 1);
        Иначе
            Частоты[Символ] = Частоты[Символ] + 1;
        КонецЕсли;
    КонецЦикла;

    Возврат Частоты;
КонецФункции

Процедура ВывестиЧастоты(Частоты) 
    // Вывод частот каждого символа
    Для Каждого ЭлементСоответствия Из Частоты Цикл
        Сообщить(Строка(ЭлементСоответствия.Ключ) + " - " + ЭлементСоответствия.Значение);
    КонецЦикла;
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
    // Исходная строка
    Строка = "Привет мир, мир хороший мир! 123 !@# ABC abc";

    // Подсчитываем частоты символов
    Частоты = НайтиЧастотыСимволов(Строка);

    // Печатаем результат
    ВывестиЧастоты(Частоты);
КонецПроцедуры
