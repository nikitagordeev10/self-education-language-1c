#include "lib1c.h"

Функция(ЦелоеЧисло, РассчитатьНалог, ЦелоеЧисло доход) 
    Возврат доход * 0.13;
КонецФункции

Процедура ЗавестиТоварНаСклад(Строка товар) 
    printf("Товар %s заведен на склад.\n", товар);
КонецПроцедуры

int main() {
    // Переменные
    Перем(Строка, СообщПривет) = "Привет, это журнал «Код»";
    printf("%s\n", СообщПривет);

    Перем(ЦелоеЧисло, Итог) = 10;
    Если(Итог == 10) Тогда
        printf("Итог равен 10.\n");
    Иначе
        printf("Итог не равен 10.\n");
    КонецЕсли

    // Работа с массивами
    Перем(Массив *, Числа) = НовыйМассив(3);
    УстановитьЭлемент(Числа, 0, (void *)(long)5);
    printf("Первый элемент: %ld\n", (long)ПолучитьЭлемент(Числа, 0));
    УдалитьМассив(Числа);

    // Работа с циклами
    Для(1, 5, НомерСотрудника)
        printf("Сотрудник №%d\n", НомерСотрудника);
    КонецЦиклаДля

    // Вызов функции
    ЦелоеЧисло Налог = РассчитатьНалог(100000);
    printf("Налог: %d\n", Налог);

    // Вызов процедуры
    ЗавестиТоварНаСклад("Холодильник");

    return 0;
}
