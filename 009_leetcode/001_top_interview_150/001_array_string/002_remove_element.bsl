// 27. Удаление элемента
// Дан целочисленный массив nums и целое число val. Удалите все вхождения val в nums "на месте". 
// Порядок элементов может быть изменён. Затем верните количество элементов в nums, которые не равны val.

// Пусть количество элементов в nums, которые не равны val, равно k. Чтобы решение было принято, вам нужно сделать следующее:
// Измените массив nums таким образом, чтобы первые k элементов массива nums содержали элементы, которые не равны val. 
// Остальные элементы массива nums не важны, как и размер массива nums. Верните k.

// Пример 1:
// Входные данные: nums = [3,2,2,3], val = 3
// Выходные данные: 2, nums = [2,2,_,_]
// Пояснение: ваша функция должна возвращать k = 2, при этом первые два элемента nums должны быть равны 2.
// Не имеет значения, что вы оставляете после возвращаемого k (поэтому это подчеркивания).

// Пример 2:
// Входные данные: nums = [0,1,2,2,3,0,4,2], val = 2
// Выходные данные: 5, nums = [0,1,4,0,3,_,_,_]
// Пояснение: ваша функция должна возвращать k = 5, при этом первые пять элементов массива nums должны содержать 0, 0, 1, 3 и 4.

// Обратите внимание, что пять элементов могут быть возвращены в любом порядке.
// Неважно, что вы оставляете после возвращённого k (поэтому это подчёркивания).

// Ограничения:
// 0 <= nums.length <= 100
// 0 <= nums[i] <= 50
// 0 <= val <= 100

// 27. Remove Element // LeetCode URL: https://leetcode.com/problems/remove-element/?envType=study-plan-v2&envId=top-interview-150

Функция УдалитьЭлемент(Массив, ЗначениеДляУдаления)

    Если Массив.Количество() = 0 Тогда
        Возврат 0;
    КонецЕсли;

    // Указатель для следующей позиции, куда будем записывать элемент
    ПозицияЗаписи = 0;

    Для Каждого Элемент Из Массив Цикл
        Если Число(Элемент) <> Число(ЗначениеДляУдаления) Тогда
            Массив[ПозицияЗаписи] = Элемент; 
            ПозицияЗаписи = ПозицияЗаписи + 1; 
        КонецЕсли;
    КонецЦикла;

    // Возвращаем количество элементов, не равных ЗначениеДляУдаления
    Возврат ПозицияЗаписи;
КонецФункции

Процедура ПриНачалеРаботыСистемы()
    // Пример 1
    Массив1 = Новый Массив;
    Массив1 = СтрРазделить("3,2,2,3", ",");
    КоличествоОставшихся1 = УдалитьЭлемент(Массив1, 3);
    Сообщить("Количество оставшихся элементов: " + КоличествоОставшихся1);
    Сообщить("Массив после удаления: " + СтрСоединить(Массив1, ", "));

    // Пример 2
    Массив2 = Новый Массив;
    Массив2 = СтрРазделить("0,1,2,2,3,0,4,2", ",");
    КоличествоОставшихся2 = УдалитьЭлемент(Массив2, 2);
    Сообщить("Количество оставшихся элементов: " + КоличествоОставшихся2);
    Сообщить("Массив после удаления: " + СтрСоединить(Массив2, ", "));
КонецПроцедуры