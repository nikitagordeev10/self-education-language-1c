// 88. Слияние отсортированных массивов
// 
// Вам даны два целочисленных массива nums1 и nums2, отсортированных в порядке неубывания, 
// и два целых числа m и n, представляющих количество элементов в массивах nums1 и nums2 соответственно.
// 
// Объедините массивы nums1 и nums2 в один массив, отсортированный в порядке неубывания.
// 
// Окончательный отсортированный массив не должен возвращаться функцией, а должен храниться в массиве nums1. 
// Для этого массив nums1 имеет длину m + n, где первые m элементов обозначают элементы, которые нужно объединить, 
// а последние n элементов равны 0 и должны быть проигнорированы. Массив nums2 имеет длину n.
// 
// Пример 1:
// Входные данные: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
// Вывод: [1,2,2,3,5,6]
// Объяснение: массивы, которые мы объединяем, — это [1,2,3] и [2,5,6].
// Результатом объединения является [1,2,2,3,5,6], где подчеркнутые элементы взяты из nums1.

// Пример 2:
// Входные данные: nums1 = [1], m = 1, nums2 = [], n = 0
// Вывод: [1]
// Объяснение: массивы, которые мы объединяем, — это [1] и [].
// Результатом объединения является [1].

// Пример 3:
// Входные данные: nums1 = [0], m = 0, nums2 = [1], n = 1
// Вывод: [1]
// Объяснение: массивы, которые мы объединяем, — это [] и [1].
// Результатом объединения является [1].
// Обратите внимание, что, поскольку m = 0, в массиве nums1 нет элементов. Цифра 0 нужна только для того, чтобы результат слияния поместился в массив nums1.

// Ограничения: 
// длина nums1 == m + n
// длина nums2 == n
// 0 <= m, n <= 200
// 1 <= m + n <= 200
// -10^9 <= nums1[i], nums2[j] <= 10^9

// 88. Merge Sorted Array // LeetCode URL: https://leetcode.com/problems/merge-sorted-array/?envType=study-plan-v2&envId=top-interview-150

Процедура СлияниеМассивов(Массив1, ДлинаМ1, Массив2, ДлинаМ2)
    // Индексы для итерации
    Индекс1 = ДлинаМ1 - 1; // Последний элемент в Массив1
    Индекс2 = ДлинаМ2 - 1; // Последний элемент в Массив2
    ПозицияЗаписи = ДлинаМ1 + ДлинаМ2 - 1; // Индекс для записи результата в Массив1

    // Слияние массивов, начиная с конца
    Пока Индекс1 >= 0 И Индекс2 >= 0 Цикл
        Если Массив1[Индекс1] > Массив2[Индекс2] Тогда
            Массив1[ПозицияЗаписи] = Массив1[Индекс1];
            Индекс1 = Индекс1 - 1;
        Иначе
            Массив1[ПозицияЗаписи] = Массив2[Индекс2];
            Индекс2 = Индекс2 - 1;
        КонецЕсли;
        ПозицияЗаписи = ПозицияЗаписи - 1;
    КонецЦикла;

    // Если остались элементы в Массив2, копируем их
    Пока Индекс2 >= 0 Цикл
        Массив1[ПозицияЗаписи] = Массив2[Индекс2];
        Индекс2 = Индекс2 - 1;
        ПозицияЗаписи = ПозицияЗаписи - 1;
    КонецЦикла;

    // Вывод результата
    Сообщение = "Результат слияния: " + СтрСоединить(Массив1, " ");
    Сообщить(Сообщение);
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
    // Пример 1
    Массив1 = Новый Массив;
    Массив1 = СтрРазделить("1,2,3,0,0,0", ",");
    Массив2 = Новый Массив;
    Массив2 = СтрРазделить("2,5,6", ",");
    СлияниеМассивов(Массив1, 3, Массив2, 3); 

    // Пример 2
    Массив3 = Новый Массив;
    Массив3.Добавить(1);
    Массив4 = Новый Массив;
    СлияниеМассивов(Массив3, 1, Массив4, 0); 

    // Пример 3
    Массив5 = Новый Массив;
    Массив5.Добавить(0);
    Массив6 = Новый Массив;
    Массив6.Добавить(1);
    СлияниеМассивов(Массив5, 0, Массив6, 1); 
КонецПроцедуры