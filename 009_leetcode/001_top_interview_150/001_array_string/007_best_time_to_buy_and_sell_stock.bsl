
// 121. Лучшее время для покупки и продажи акций
// Вам дан массив цен, где prices[i] — это цена определённой акции в i-й день.
// Вы хотите максимизировать свою прибыль, выбрав один день для покупки одной акции и другой день в будущем для продажи этой акции.
// Верните максимальную прибыль, которую вы можете получить от этой сделки. Если вы не можете получить никакой прибыли, верните 0.

// Пример 1:
// Входные данные: prices = [7,1,5,3,6,4]
// Выходные данные: 5
// Пояснение: покупаем на второй день (price = 1) и продаем на пятый день (price = 6), прибыль = 6-1 = 5.
// Обратите внимание, что покупка на второй день и продажа на первый день недопустимы, потому что сначала нужно купить, а потом продать.

// Пример 2:
// Входные данные: prices = [7,6,4,3,1]
// Выходные данные: 0
// Пояснение: В этом случае транзакции не выполняются, а максимальная прибыль равна 0. 

// Ограничения:
// 1 <= prices.length <= 105
// 0 <= prices[i] <= 104

// 121. Best Time to Buy and Sell Stock // LeetCode URL: https://leetcode.com/problems/best-time-to-buy-and-sell-stock/?envType=study-plan-v2&envId=top-interview-150

Функция МаксимальнаяПрибыль(Цены)
    // Инициализация переменных
    МинимальнаяЦена = Неопределено; // Минимальная цена, которую мы встретили
    МаксимальнаяПрибыль = 0; // Максимальная прибыль, которую мы можем получить

    // Проходим по массиву цен
    Для Каждого ТекущаяЦена Из Цены Цикл
        // Если минимальная цена не определена или текущая цена меньше минимальной
        Если МинимальнаяЦена = Неопределено ИЛИ ТекущаяЦена < МинимальнаяЦена Тогда
            МинимальнаяЦена = ТекущаяЦена; // Обновляем минимальную цену
        КонецЕсли;

        // Вычисляем потенциальную прибыль
        Прибыль = ТекущаяЦена - МинимальнаяЦена;

        // Если прибыль больше максимальной, обновляем максимальную прибыль
        Если Прибыль > МаксимальнаяПрибыль Тогда
            МаксимальнаяПрибыль = Прибыль;
        КонецЕсли;
    КонецЦикла;

    // Возвращаем максимальную прибыль
    Возврат МаксимальнаяПрибыль;
КонецФункции

// Пример использования
Процедура ПриНачалеРаботыСистемы()
    // Пример 1
    Массив1 = Новый Массив;
	Массив1 = СтрРазделить("7,1,5,3,6,4", ",");
    Прибыль1 = МаксимальнаяПрибыль(Массив1);
    Сообщить("Максимальная прибыль: " + Прибыль1);

    // Пример 2
    Массив2 = Новый Массив;
	Массив2 = СтрРазделить("7,6,4,3,1", ",");
    Прибыль2 = МаксимальнаяПрибыль(Массив2);
    Сообщить("Максимальная прибыль: " + Прибыль2);
КонецПроцедуры