Процедура ПузырьковаяСортировка(МассивСлов)
	
	Пока Истина Цикл
		БылиПерестановки = Ложь;
		Для Индекс = 1 По МассивСлов.Количество() - 1 Цикл  
			Если МассивСлов[Индекс - 1] > МассивСлов[Индекс] Тогда  
				ВременноеЗначение = МассивСлов[Индекс - 1];
				МассивСлов[Индекс - 1] = МассивСлов[Индекс];
				МассивСлов[Индекс] = ВременноеЗначение; 
				БылиПерестановки = Истина;
			КонецЕсли;
		КонецЦикла;
		
		Если НЕ БылиПерестановки Тогда
			Прервать;    
		КонецЕсли;  
	КонецЦикла;     
	
	ОтсортированныйТекст = "";
	Для Каждого Слово Из МассивСлов Цикл 
		ОтсортированныйТекст = ОтсортированныйТекст + Слово + " ";
	КонецЦикла;      
	
	Сообщить(ОтсортированныйТекст);
КонецПроцедуры 

Процедура РазделитьТекстНаСлова(Текст)
	
	Текст = НРег(Текст);
	СимволыПрепинания = "`~!@#$%^&*()-=_+\|[]{};':\"",.<>/?";
	МассивСлов = Новый Массив;
	ТекущееСлово = "";
	ВнутриСлова = Ложь;
	
	Для Индекс = 1 По СтрДлина(Текст) Цикл
		Символ = Сред(Текст, Индекс, 1);
		
		Если Символ = " " Или СтрНайти(СимволыПрепинания, Символ) > 0 Тогда			
			Если ВнутриСлова Тогда
				МассивСлов.Добавить(ТекущееСлово); 
				ТекущееСлово = "";
			КонецЕсли;
			ВнутриСлова = Ложь;
		Иначе
			ВнутриСлова = Истина;
			ТекущееСлово = ТекущееСлово + Символ;
		КонецЕсли;
	КонецЦикла;

	Если ВнутриСлова Тогда 
		МассивСлов.Добавить(ТекущееСлово);
	КонецЕсли;  
	
	ПузырьковаяСортировка(МассивСлов);
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
	ВходнойТекст = "";
	ВвестиСтроку(ВходнойТекст);
	РазделитьТекстНаСлова(ВходнойТекст);
КонецПроцедуры