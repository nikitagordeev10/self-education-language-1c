// Инверсия регистра

Функция ИнвертироватьРегистр(Строка) Экспорт
    ДлинаСтроки = СтрДлина(Строка);
    Результат = "";

    Для Индекс = 1 По ДлинаСтроки Цикл
        Символ = Сред(Строка, Индекс, 1);

        // Проверяем, является ли символ буквой верхнего регистра
        Если Символ = ВРег(Символ) Тогда
            Результат = Результат + НРег(Символ); // Меняем на нижний регистр
        ИначеЕсли Символ = НРег(Символ) Тогда
            Результат = Результат + ВРег(Символ); // Меняем на верхний регистр
        Иначе
            Результат = Результат + Символ; // Оставляем символ без изменений
        КонецЕсли;
    КонецЦикла;

    Возврат Результат;
КонецФункции

Процедура ПриНачалеРаботыСистемы()
    // Исходная строка
    ИсходнаяСтрока = "Функция для управления регистром строки в языке 1С";

    // Инвертируем регистр
    ИнверсированнаяСтрока = ИнвертироватьРегистр(ИсходнаяСтрока);

    // Вывод результата
    Сообщить(ИнверсированнаяСтрока);
КонецПроцедуры