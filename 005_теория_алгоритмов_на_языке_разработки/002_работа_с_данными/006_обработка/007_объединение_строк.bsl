Функция ОбъединениеСтрок(Строка1, Строка2)
	Возврат Строка1 + " " + Строка2;
КонецФункции

Процедура ПриНачалеРаботыСистемы()
	Строка1 = "";
	ВвестиСтроку(Строка1);

	Строка2 = "";
	ВвестиСтроку(Строка2);

	ОткрытьЗначение(ОбъединениеСтрок(Строка1, Строка2));
КонецПроцедуры
