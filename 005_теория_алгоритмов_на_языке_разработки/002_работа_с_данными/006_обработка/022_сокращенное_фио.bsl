Процедура СокращенноеФИО(ПолноеФИО)
	Строки = СтрЗаменить(ПолноеФИО, " ", Символы.ПС);
	Фамилия = СтрПолучитьСтроку(Строки, 1);
	Имя = СтрПолучитьСтроку(Строки, 2);
	Отчество = СтрПолучитьСтроку(Строки, 3);

	Сообщить(Фамилия + " " + Сред(Имя, 1, 1) + ". " + Сред(Отчество, 1, 1) + ".");
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
	ПолноеФИО = "";
	ВвестиСтроку(ПолноеФИО);
	СокращенноеФИО(ПолноеФИО);
КонецПроцедуры