Процедура ПриНачалеРаботыСистемы()
	Числа = Новый Массив(5);
	
	Для ЭлемМассива = 0 По 4 Цикл
		ВвестиЧисло(Числа[ЭлемМассива]);	
	КонецЦикла;
	

	СуммаЧиселПользователя = 0;
	Для ЭлемМассива = 0 По 4 Цикл
		СуммаЧиселПользователя = СуммаЧиселПользователя + Числа[ЭлемМассива];
	КонецЦикла;

	ОткрытьЗначение("Сумма чисел равна " + СуммаЧиселПользователя);

КонецПроцедуры