Функция КоличествоПробеловВСтроке(Сообщение)
	
	КоличествоПробелов = 0;
	Для ПозицияСимвола = 1 По СтрДлина(Сообщение) Цикл   
		Символ = Сред(Сообщение, ПозицияСимвола, 1);
		Если Символ = " " Тогда
			КоличествоПробелов = КоличествоПробелов + 1;
		КонецЕсли;
	КонецЦикла;                 
	
	Возврат(КоличествоПробелов);

КонецФункции

Процедура ПриНачалеРаботыСистемы()
	
	 Строка = "";
	 ВвестиСтроку(Строка);
	 Сообщить(КоличествоПробеловВСтроке(Строка));
	
КонецПроцедуры
