// Программа для проверки, является ли число палиндромом 
// Автор: Гордеев Никита, 2024

Функция РазвернутоеЧисло(Число)

    // Переменная для хранения результата
    РазвернутоеЧисло = 0;

    // Флаг для определения знака
    Отрицательное = Ложь;
    Если Число < 0 Тогда
        Отрицательное = Истина;
        Число = -Число; // Делаем число положительным для обработки
    КонецЕсли;

    // Пока число не равно нулю
    Пока Число <> 0 Цикл
        // Добавляем последнюю цифру числа в развернутое число
        РазвернутоеЧисло = РазвернутоеЧисло * 10 + (Число % 10);

        // Убираем последнюю цифру из числа
        Число = Цел(Число / 10);
    КонецЦикла;

    // Восстанавливаем знак, если число было отрицательным
    Если Отрицательное Тогда
        РазвернутоеЧисло = -РазвернутоеЧисло;
    КонецЕсли;

	Возврат РазвернутоеЧисло;

КонецФункции

Функция ЯвляетсяПалиндромом(Число)
	// Отрицательные числа не являются палиндромами
	Если Число < 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Строка(Число) = Строка(РазвернутоеЧисло(Число));
КонецФункции

Процедура ПриНачалеРаботыСистемы()

	Число = 121;
	Сообщить("Является ли " + Число + " простым числом? " + ЯвляетсяПалиндромом(Число));
	
КонецПроцедуры

