Функция ВывестиЧислаВДиапазоне(Число1, Число2)
	СтрокаСЧислами = "";
	Для ЧислоВДиапазоне = Число1 По Число2 Цикл
		СтрокаСЧислами = СтрокаСЧислами + ЧислоВДиапазоне + " ";
	КонецЦикла;
	ОткрытьЗначение(СтрокаСЧислами);
КонецФункции

Процедура ПриНачалеРаботыСистемы()
	Число1 = 0;
	ВвестиЧисло(Число1);
	
	Число2 = 0;
	ВвестиЧисло(Число2);

	Сообщить(ВывестиЧислаВДиапазоне(Число1, Число2));
КонецПроцедуры

